export interface FormData {
  email: string;
  firstName: string;
  lastName: string;
  phone: string;
}

export interface EmailFormData {
  email: string;
  name: string;
  phone: string;
  message: string[];
}
